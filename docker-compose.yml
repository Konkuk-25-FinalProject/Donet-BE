version: "3.8"

services:
  blue-server:
    image: jangikhwan/donet-be
    container_name: blue-server
    ports:
      - "8081:8080"
    env_file:
      - .env

  green-server:
    image: jangikhwan/donet-be
    container_name: green-server
    ports:
      - "8082:8080"
    env_file:
      - .env

volumes:
  dbdata:
